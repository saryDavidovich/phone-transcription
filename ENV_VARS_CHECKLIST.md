# משתני סביבה ופעולות ידניות - עדכון "ניהול מוסד"

## משתני סביבה חדשים שצריך להוסיף בריילוואי

| משתנה | ערך | הערה |
|---|---|---|
| `NEDARIM_MOSAD` | מספר המוסד שלכם בנדרים פלוס (7 ספרות) | לבדיקה זמנית: `0` |
| `NEDARIM_API_VALID` | טקסט האימות (ApiValid) - במסך "עוד > מפתחות API" בנדרים פלוס | לבדיקה זמנית: `j+iyEFN3bE` |
| `APP_URL` | הכתובת הציבורית של האתר, לדוגמה `https://yourapp.up.railway.app` | חובה - נדרים פלוס שולחים אליה את אישור התשלום |
| `APP_BASE_URL` | **כנראה כבר קיים אצלכם** (בשימוש ב-test_transcribe הקיים) - אותה כתובת כמו APP_URL | ודאו שהוא מוגדר, נדרש עכשיו גם ללשונית "יצירת תמלול" |

## פעולות ידניות שאתה צריך לעשות

1. **אצל נדרים פלוס עצמם**: להוציא מספר מוסד (Mosad) אמיתי + ApiValid מהמסך "עוד > מפתחות API" שלהם, ולהחליף בערכים האמיתיים (לא ערכי הבדיקה).
2. **מיגרציית בסיס הנתונים**: תרוץ אוטומטית בפריסה הבאה (`prisma`-style `ADD COLUMN IF NOT EXISTS`, אותו דפוס שכבר קיים אצלכם ב-`_migrate_db()`) - אין צורך בפעולה ידנית, רק לוודא שהפריסה מצליחה (בדקתי מול Postgres אמיתי, עובד).
3. **יצירת המוסד הראשון**: אחרי הפריסה, היכנס למנהל-על → "מוסדות" → צור מוסד → תקבל קוד כניסה זמני בהודעת ה-flash. תן אותו למוסד.
4. **חיוב אוטומטי דרך טוקן שמור** (הוראת קבע / חיוב שקט ברקע) **אינו מומש** - נדרים פלוס דורשים אישור ידני נפרד לזה (כתבו זאת מפורשות בתיעוד). כרגע כל תשלום עובר דרך האייפרם שלהם עם הזנת כרטיס בפועל. אם תרצו את זה בעתיד - לפנות ל-office@nedar.im.
5. **הרחבת ה-IVR (שלוחה 7 + מספר תלמיד) עדיין לא בוצעה** - repo נפרד (`phone-transcription-ivr`), יש לתאם המשך עבודה נפרד.

## קבצים חדשים שנוספו (לוודא שהם עולים ב-push)
```
routes/institution.py
routes/institution_billing.py
routes/institution_transcribe.py
routes/institution_students.py
templates/institution/*.html   (8 קבצים)
templates/admin/institutions.html
templates/admin/institution_detail.html
```

## קבצים קיימים ששונו (העלאה מלאה מחליפה, לא מיזוג ידני)
```
models.py
app.py
routes/admin.py
templates/admin/base.html
```

---

## עדכון נוסף: Webhook כללי של נדרים פלוס + כניסה עם Google + תיוג "תמלול פון"

### 1. וודאו ש-APP_URL מוגדר נכון וב-https
זה כנראה **הסיבה בפועל** לכך שיתרת מוסד לא מתעדכנת אחרי תשלום: אם `APP_URL`
לא מוגדר (או מוגדר עם `http://` ולא `https://`), הכתובת שנשלחת לנדרים פלוס
כ-CallBack יוצאת שגויה, והם פשוט לא יכולים לשלוח אליה עדכון. הוספתי גם
הגנה טכנית נוספת (ProxyFix + כפיית https), אבל **וודאו קודם כל שב-Railway
משתנה הסביבה `APP_URL` מוגדר לכתובת הציבורית האמיתית של האתר** (עם https).

### 2. הגדירו Webhook קבוע ברמת המוסד אצל נדרים פלוס (חדש - חשוב!)
בממשק נדרים פלוס: **עוד > Webhook** → בשדה "עדכוני עסקאות" הזינו:
```
https://<הדומיין שלכם>/payment/nedarim/webhook
```
זה משלים את הפער: מעכשיו **כל** תשלום מוצלח תחת מספר המוסד שלכם בנדרים
פלוס (כולל תשלומים שלא הוקמו דרך ה-API שלנו - למשל תשלום ידני מהדשבורד
שלהם, או קישור התשלום האישי החדש ללקוחות בודדים) ישלח עדכון אוטומטי,
ולא רק תשלומי מוסדות שהוקמו מפורשות דרך "טעינת יתרה" בממשק המוסד.
ה-Webhook הזה מזכה יתרה **רק** לתשלומים עם קטגוריה (Groupe) "תמלול פון" -
ולא נוגע בשום תשלום אחר שאולי קיים תחת אותו מספר מוסד.

### 3. קישור אישי ללקוח בודד לטעינת יתרה
כל לקוח (לא רק מוסד) יכול עכשיו לקבל קישור אישי לטעינה בנדרים פלוס:
```
https://<הדומיין שלכם>/payment/nedarim/topup-link/<מספר-טלפון-של-הלקוח>
```
זה מפנה אותו ישירות לדף התשלום המתארח של נדרים פלוס עם הטלפון וקטגוריית
"תמלול פון" ממולאים מראש - בלי שום פיתוח נוסף, בלי אייפרם. אפשר לשלוח
קישור כזה ב-SMS/מייל, או לקשר אליו משלוחת IVR. הזיכוי בפועל קורה
כשמגיע העדכון מה-Webhook שהגדרתם בסעיף 2.

### 4. כניסה עם Google - משתני סביבה חדשים
| משתנה | ערך | הערה |
|---|---|---|
| `GOOGLE_OAUTH_CLIENT_ID` | ה-Client ID מ-Google Cloud Console | ראו הוראות בהודעת הצ'אט |
| `GOOGLE_OAUTH_CLIENT_SECRET` | ה-Client Secret מ-Google Cloud Console | סוד - לא לחשוף בצד לקוח |

ה-Authorized redirect URI שיש להגדיר אצל Google:
```
https://<הדומיין שלכם>/institution/auth/google/callback
```
